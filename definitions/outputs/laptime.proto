syntax = "proto3";

package protobuf_msgs;

option go_package = "ase/pb_outputs";

//
// Messages that can be outputed by a lap time sensor module
//

message LapTimeOutput {
    uint64 lapTime = 1;   // Time of complete lap in ms
    uint64 lapStartTime = 2; // Start of the lap
}
