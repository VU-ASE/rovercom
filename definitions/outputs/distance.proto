syntax = "proto3";

package protobuf_msgs;

option go_package = "ase/pb_outputs";

//
// Messages that can be outputed by a distance sensor module
//

message DistanceSensorOutput {
    // distance in meters
    float distance = 1;
}